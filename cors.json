{
  "rules": [
    {
      "allowed": {
        "origins": ["*"],
        "methods": ["GET", "PUT", "POST", "DELETE", "HEAD"],
        "headers": ["*"]
      },
      "maxAgeSeconds": 3000
    }
  ]
}
